<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="1">
    <record id="action_force_confirm_quotation" model="ir.actions.server">
        <field name="name">Force Confirm Quotation</field>
        <field name="model_id" ref="sale.model_sale_order" />
        <field name="state">code</field>
        <field name="code">
if rds:
    rds.with_context(use_quotation_confirm_wizard=False).action_confirm()
    on = {'type': 'ir.actions.client', 'tag': 'reload'}
        </field>
    </record>
</odoo>
